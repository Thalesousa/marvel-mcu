import styled from 'styled-components';

export const Context = styled.div`
  max-width: 1200px;
  padding: 60px 10px 0;

  button {
    border: none;
    background: none;
    font-size: 0;
    cursor: pointer;
  }
`